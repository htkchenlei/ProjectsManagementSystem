[project]
requires = ["python-3.12"]

[dev]
build = ["pip", "install", "-r", "requirements.txt"]
run = ["python", "app.py"]

[deploy]
build = ["pip", "install", "-r", "requirements.txt"]
run = ["python", "app.py"]
